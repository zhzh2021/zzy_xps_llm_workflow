reference_library:
  # Default reference library (Tool-level)
  default_dir: "standards_library"
  # Optional project override (if exists, preferred)
  project_override_dir: "project_root/standards_library"

references:
  Fe2+:
    filename: "Fe2_reference.csv"
    energy_column: "energy"
    mu_column: "mu_normalized"
    shift_eV: 0.0
  Fe3+:
    filename: "Fe3_reference.csv"
    energy_column: "energy"
    mu_column: "mu_normalized"
    shift_eV: 0.0
